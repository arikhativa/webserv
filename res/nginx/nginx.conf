
user nginx;
worker_processes auto;
pid /run/nginx.pid;

events {
  worker_connections  1024; 
}

http {
	server {
		listen 0.0.0.0:80;
		root /var/www/html;
		index file.html;
		return 501;
	
		location / {
		}
		location /aaa/ {
			index index.html;
		}
		location /bbb/ {
		}
	}
}
