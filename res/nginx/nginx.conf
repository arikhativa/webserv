
user nginx;
worker_processes auto;
pid /run/nginx.pid;

events {
  worker_connections  1024; 
}

http {
	server {
		listen 0.0.0.0:80;
		root /var/www/html;
		location / {
			index index.html;
		}
		location /aaa/ {
			index index2.html;
		}
	}
}
